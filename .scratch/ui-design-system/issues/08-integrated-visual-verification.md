# 08 — 整体视觉回归和样式债清理

**What to build:** 完成四个功能页面迁移后的整体视觉回归，清理剩余散落样式，确认本地日历在移动端没有文字溢出、重叠或明显风格割裂。完成后项目通过测试、分析和中文乱码检查，可以作为下一轮功能建设的统一 UI 基础。

**Blocked by:** 04 — 日历页视觉系统迁移；05 — 日程页视觉系统迁移；06 — 待办页视觉系统迁移；07 — 我的页视觉系统迁移

**Status:** resolved

- [x] 清理功能页面中剩余可迁移的孤立颜色、文字、间距、圆角和阴影样式。
- [x] 保留必要的特殊视觉值时，有明确理由且不会破坏设计系统边界。
- [x] 四个功能页面在移动端窄屏下没有文字溢出、按钮文字截断或 UI 重叠。
- [x] 四个功能页面共享统一视觉语言，同时保留各自功能重点。
- [x] 现有事项、待办项、本地数据、月份切换、删除确认和清空本地数据行为仍然通过测试。
- [x] 运行 `flutter test` 并通过。
- [x] 运行 `flutter analyze` 并通过。
- [x] 检查代码和文档中是否有 UTF-8/中文乱码。
- [x] 如发现乱码，修复后重新运行相关验证。

## Answer

已完成四页视觉系统回归：日历页、日程页、待办页和我的页均使用共享页面容器、页面标题、内容卡片、空状态、统计行、底部表单或危险操作组件。功能页中可迁移的旧文字样式、旧颜色和固定间距已收敛到 theme token；剩余裸色值限定在 theme token 定义、事项颜色数据、持久化默认值和插画绘制中，均有明确用途。

新增 `test/app_theme_test.dart` 覆盖日历页、日程页、待办页、我的页 token 消费，以及 320px 窄屏四页切换无布局异常。验证已通过：`flutter test test\app_theme_test.dart`、`flutter test test\widget_test.dart`、`flutter test`、`flutter analyze`，并完成中文/UTF-8 乱码扫描。
